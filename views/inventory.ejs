<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Inventory Management List</title>
    <link rel="stylesheet" href="css/style.css">
</head>
<body>
    <h1>Inventory List</h1>

    <form action="/inventory/createItem" method="POST">
        <input type="text" placeholder="Enter Item" name="inventoryItem">
        <input type="submit" value="Add to List">
    </form>

    <ul>
    <% items.forEach(el => { %>
        <li class="inventoryItem" data-id="<%= el._id %>">
            <span class="<%= el.completed === true ? "completed" : "not"%>"><%= el.item %></span>
            <span class="del">Delete</span>
        </li>
    <% }) %>
    </ul>

    <h2><%= user.displayName %> has <%= left %> left to get:</h2>

    <a href="../auth/logout">Logout</a>

    <script src="js/main.js"></script>
</body>
</html>